# =========================================================
# CMEPDA Project 2024 - Dependencies Manifesto
# =========================================================

# --- 1. Deep Learning & Medical Imaging ---
# PyTorch (Note: Per accelerazione hardware, installare CUDA/MPS separatamente)
torch>=2.0.0
torchvision>=0.15.0
monai>=1.3.0

# --- 2. Machine Learning & Statistics ---
scikit-learn>=1.2.2
statsmodels>=0.14.0

# --- 3. Data Manipulation & Math ---
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# --- 4. Neuroimaging & Optical Rendering ---
nibabel>=5.1.0
matplotlib>=3.7.0

# --- 5. CI/CD, Testing & Documentation ---
pytest>=7.3.1
pytest-cov>=4.0.0
sphinx>=7.0.0
sphinx-rtd-theme>=1.2.0

# --- 6. MATLAB Integration (Bridge) ---
# NOTA: matlabengine richiede l'installazione locale di MATLAB (R2023a+).
# Se pip install fallisce per questioni di licenza, eseguire dal terminale:
# cd "C:\Program Files\MATLAB\R202XC\extern\engines\python" && python setup.py install
matlabengine>=9.14.0